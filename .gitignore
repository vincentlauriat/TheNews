# ── macOS ──────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── Xcode ──────────────────────────────────────────────
build/
DerivedData/
*.xcuserstate
*.xcscmblueprint
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Release artifacts (published on the Releases page, not versioned)
*.dmg
dmg-staging/

# Project generated by XcodeGen — regenerate with `xcodegen generate`
*.xcodeproj/

# ── Swift Package Manager ──────────────────────────────
.build/
.swiftpm/

# ── Sparkle CLI tools (downloaded by Scripts/release.sh, not versioned) ─
.sparkle-tools/

# ── Tooling / local state (not for publication) ────────
.omc/
.claude/

# ── Private working docs (kept local, may contain personal info) ─
CLAUDE.md
COMMANDS.md
CHANGES.md
MEMORY.md
PLAN.md
TODOS.md
